{
  "name": "demo-capture",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Release Gate path driver, two-stage Compose probe ring, frozen Watch plan, capture/reset scripts, and the strict saved export for the two saved Demo Runs (issue #19).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "link": "bash scripts/link.sh",
    "capture": "bun run capture.ts"
  },
  "dependencies": {
    "@grpc/grpc-js": "1.13.2",
    "@grpc/proto-loader": "0.7.15"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "22.20.1",
    "typescript": "6.0.3"
  }
}
