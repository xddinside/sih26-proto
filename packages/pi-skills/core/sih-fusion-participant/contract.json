{
  "name": "sih-fusion-participant",
  "version": "1.0",
  "stage": "diagnose",
  "tool_group": "diagnose-read-only",
  "access": "Read: Read Broker metric/trace/log/code queries only. Network: none beyond the allow-listed docs proxy (context only, never evidence). No writes, no shell, no open web; no direct production access, credentials, or actions.",
  "independence": "Parallel, isolated scratch, no peer visibility, cannot communicate; citations must reference revision R_n only.",
  "scope": "demo",
  "output_schema": {
    "name": "fusion-participant-output",
    "version": "1.0"
  },
  "retry": {
    "malformed": "rerun-once-then-needs-human",
    "timeout": "rerun-once-then-needs-human"
  },
  "demo_subset": true
}
